# ============================================================
# CI：push 到 main 时自动验证 + 发布 Docker 镜像
#   job 1: build  —— Node 前端构建检查（pnpm build）
#   job 2: docker —— 构建并推送镜像到 ghcr.io/mogvl/bagua:latest
#                    （绿联 NAS 上 docker compose up -d 直接拉取）
#   PR 触发时仅验证，不推送镜像
#
# 注意：项目源码在 mingyu/ 子目录（根目录无 package.json），
#       因此用 pnpm/action-setup 显式安装 pnpm，并在 mingyu/ 下执行。
# ============================================================
name: ci

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read
  packages: write

jobs:
  build:
    name: pnpm build
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: mingyu
    steps:
      - uses: actions/checkout@v5
      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 11.9.0
      - uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: pnpm
          cache-dependency-path: mingyu/pnpm-lock.yaml
      - name: Install dependencies
        run: pnpm install --frozen-lockfile
      - name: Build frontend
        run: pnpm build

  docker:
    name: docker image (ghcr.io/mogvl/bagua)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: ./mingyu
          push: ${{ github.event_name == 'push' }}
          tags: ghcr.io/mogvl/bagua:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max